namespace Domain.Enums;

/// <summary>
/// Насколько усердно модель «думает» перед ответом. Понимает не всякий сервер и не всякая модель:
/// если параметр проигнорируют, модель просто продолжит думать столько, сколько считает нужным.
/// </summary>
public enum ReasoningEffortEnum
{
    /// <summary>Параметр не отправляется — как решит сервер.</summary>
    Default,

    /// <summary>Рассуждения выключены. На локальной модели именно они съедают основное время ответа.</summary>
    None,

    Low,
    Medium,
    High,
}
